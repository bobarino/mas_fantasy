import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
export default class PlayerLookupScreen extends React.Component {
  render() {
    return (
      <Text>This is the Player Lookup screen!</Text>
    );
  }
}
